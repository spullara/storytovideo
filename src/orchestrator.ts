import type { PipelineOptions } from "./types";

export async function runPipeline(
  storyText: string,
  options: PipelineOptions
): Promise<void> {
  // TODO: Implement in Wave 4 — Claude orchestrator agent loop
  console.log("Orchestrator not yet implemented. Use --dry-run to test pipeline setup.");
}

